principle = float(input('investment: '))
rate = float(input('annual interest (in %): '))
time = float(input('time (in years): '))
interest= (principle * rate * time)/100
print ('your total interest will be ',interest)